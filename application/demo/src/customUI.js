import { model, getFlowProperties } from "@liquefy/flow.core";
import { div } from "@liquefy/flow.DOM";

import { textInputField as basicTextInputField, button as basicButton } from "@liquefy/basic-ui";
// import { modernButton } from "../modern/ModernButton";


export const button = basicButton;
export const textInputField = basicTextInputField;

// return modernTextField.apply(null, parameters);
// }

