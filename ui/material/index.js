import { button, icon, input } from "./src/components";
// import { icon } from "./src/icon";

export const materialTheme = {
  components: {
    button, 
    icon, 
    input
  }
}

export {  
  button,
  icon,
  input,
}
