# cascade.application

Placeholder. This is the corresponding piece to `flow.application` - example
apps built on the Cascade generation of the framework instead of Flow. Empty
for now; will hold sub-projects the same way `flow.application` holds
`demo`/`hello-world`/`experiments`, each its own package.
